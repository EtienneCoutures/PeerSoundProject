const riot = require('riot');
const jQuery = require('jquery');
const ElectronSettings = require('electron-settings');

// Save the counter value

/*if ( !ElectronSettings.get()['counter'] )
  ElectronSettings.set('counter', 0);*/
